{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(lldb) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe" ,
            "args": ["w16_projects_exam.csv", "sp16_projects_exam.csv"],
            "cwd": "${workspaceFolder}"
        }
    ]
}